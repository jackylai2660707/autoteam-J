services:
  autoteam:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - default
      - caddy_default

networks:
  default:
  caddy_default:
    external: true
